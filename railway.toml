# Deploy on https://railway.app — attach a volume at /data for SQLite (see DEPLOY.md).
[build]
builder = "DOCKERFILE"

[deploy]
# Start command is defined in Dockerfile CMD.
# Secrets are injected by Railway as runtime environment variables — never baked into the image.
