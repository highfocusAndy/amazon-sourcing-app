"use client";

/**
 * Deprecated: previously showed a Sign out button in the middle panel.
 * Kept as a no-op component so existing imports (Explorer / Analyzer headers)
 * still compile without rendering extra UI.
 */
export function HeaderAuth(): null {
  return null;
}
